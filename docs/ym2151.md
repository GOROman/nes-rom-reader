# YM2151 FM音源カートリッジ (実機検証済み)

ダンパーのカートリッジスロットに「YM2151 を載せたポパイ(HVC-PP)改造カートリッジ」を
挿して、実チップの FM 音源を鳴らす。X68000 の MDX 曲データの再生に対応。

- ファーム: `feature/ym2151` ブランチ (famidump v0.6)
- リーダー基板は **v0.1 のまま完全無改造**(U6 DIR 改造・BUS_DIR 不要)
- φM = **4MHz**(X68000 ネイティブ。ピッチ補正不要)

## 全体接続図

```
 M5Stamp S3            リーダー基板 v0.1                ポパイ改造カート (HVC-PP)
┌──────────┐        ┌──────────────────┐            ┌─────────────────────────┐
│          │  SR    │ 74HCT595 U2/U3   │ CPU A0-A10 │  PRG ROM (28pin, /CE切断)│
│ G12/13/14├───────→│ (アドレス, 5V)    ├───────────→│  の足から YM2151 へ       │
│          │        └──────────────────┘  エッジ5-13 │                          │
│          │        ┌──────────────────┐            │   ┌────────────────┐    │
│ G42 (M2) ├───────→│ 74HCT541 U8 (5V) ├───────────→│──→│φM(24)  YM2151  │    │
│          │ 4MHz   └──────────────────┘  エッジ32   │   │                │    │
│          │        ┌──────────────────┐            │   │ SO(21)─┐       │    │
│ G4  MD0 ←├────────┤                  │← エッジ26   │←──┼────────┼(CHR:11)│    │
│ G5  MD1 ←├────────┤ 74LVC245 U7      │← エッジ27   │←──┼─SH1(20)┼(CHR:12)│    │
│ G6  MD2 ←├────────┤ (5V→3.3V, B→A)   │← エッジ28   │←──┼─φ1(23) ┴(CHR:13)│    │
│          │        │  OE=G39          │            │   └────────────────┘    │
│          │        └──────────────────┘            │  ※CHRピンへの割り当ては   │
│          │        ┌──────────────────┐            │    順不同(自動判別)       │
│ G46 PWM ─├───────→│ R1 10k ┬ R2 20k  │            └─────────────────────────┘
│ 78kHz/9bit        │ (分圧の中点)      │
└──────────┘        └───┬──────────────┘
                        │
                        └──[10kΩ]──┬──[10nF]── GND      ← LPF fc≒16kHz
                                   └──[1µF]──→ アンプ → スピーカー
                                     (DCカット必須)
```

## 駆動方式: フルアドレスバス書き込み

YM2151 への制御はすべて **CPU アドレス線**(74HCT595 が常時 5V 駆動)で行う。
データバス(U6)を使わないため基板改造が不要。

| 役割 | アドレス線 | 補足 |
|---|---|---|
| D0-D7 | CPU A0-A7 | データ/レジスタ番号 |
| A0 | CPU A8 | レジスタ/データ選択 |
| /WR | CPU A9 | srWrite32 3回でストローブ生成(幅数十µs、YMはスタティックなので可) |
| /IC | CPU A10 | 起動直後はSRが全0=リセット状態で待機(好都合) |

BUSY は読めないため 30µs 固定ウェイト。/CS=GND・/RD=+5V 固定。

## ポパイカート側 配線表 (検証済み)

**PRG ROM (28ピン JEDEC, /CE=/ROMSEL は切断済み) の足:**

| YM2151 ピン | 信号 | PRG ROM の足 |
|---|---|---|
| 10,12,13,14,15,16,17,18 | D0-D7 | pin 10,9,8,7,6,5,4,3 (A0-A7) |
| 4 | A0 | pin 25 (A8) |
| 5 | /WR | pin 24 (A9) |
| 3 | /IC | pin 21 (A10) |
| 6 | /RD | pin 28 (+5V 固定) |
| 7 | /CS | pin 14 (GND 固定) |
| 22 | VDD | pin 28 (+5V) + パスコン 0.1µF |
| 1, 11 | VSS | pin 14 (GND) **2本とも** |
| 2, 8, 9, 19 | /IRQ, CT1, CT2, SH2 | 未接続 |

**CHR ROM (28ピン, セレクト切断済み) の足 — 音声シリアル出力:**

| YM2151 ピン | 信号 | CHR ROM の足 | 経路 |
|---|---|---|---|
| 23 | φ1 | pin 11 (D0) | → エッジ26 → U7 → G4 |
| 21 | SO | pin 12 (D1) | → エッジ27 → U7 → G5 |
| 20 | SH1 | pin 13 (D2) | → エッジ28 → U7 → G6 |

※ 3本の割り当て順は問わない。`F` 実行時にファームが信号を測って
φ1(50%クロック)/SH1(fs×2エッジ)/SO(残り)を自動判別する(`YMMAP` 表示)。

**エッジフィンガー直付け(1点のみ):**

| YM2151 ピン | 信号 | 位置 |
|---|---|---|
| 24 | φM | エッジフィンガー 32 (M2)。NROMでは未使用なのでパターンなし=フィンガーに直接ハンダ |

## YM3012 シミュレーション (ファーム内蔵)

実 YM3012 を使わず、Core 0 の専用タスクが SO/SH1/φ1 を Dedicated GPIO
(1サイクル読み)で取り込み、13bit浮動小数点(10bit仮数+3bit指数)をデコードして
**G46 に 78.125kHz/9bit PWM** を出力する(SH1側=RIGHT ch モノラル、fs=φM/64=62.5kHz)。

- フレームごとに φ1 エッジ数(31-33)を検証し、割り込み等で化けたフレームは
  破棄して直前値を保持(ノイズ対策)
- 音声取り出しはリーダー基板の **CIRAM A10 分圧 (R1=10k/R2=20k SMD) の中点**
  = 「GNDまで20kΩを示す足」。スロット側(30kΩ)や GND(0Ω)ではないことをテスターで確認

## コマンド (famidump v0.6)

```
F              -> YM初期化: φM 4MHz供給 + /ICリセット + 信号自動判別。"YMRDY"
Y <reg> <val>  -> レジスタ書き込み (hex)
Q              -> ドレミファソラシドをループ再生(次のコマンドで停止)
E              -> 内蔵曲(Ys2 エンディング2)を再生
X              -> MDXイベントストリーム再生 (host/mdx/mdxplay.py から)
A              -> G46 駆動診断 + 880Hzテストトーン(配線チェック用)
D              -> 信号診断(φ1/SH1/SOエッジ数、採用率、/ICホールド判定)
```

**スタンドアロン動作**: 電源ONから2秒以内にシリアル入力がなければ、
内蔵曲を自動でループ再生する(曲間3秒)。USB充電器だけで動く。

## MDX 再生パイプライン (host/mdx/)

```
*.MDX ──(mdxdump: portable_mdxのMXDRVでOPMレジスタ書き込みを
          タイムスタンプ付きで抽出)──→ *.evt
*.evt ──(mdxplay.py: 4バイトレコード化+フロー制御ストリーミング)──→ X コマンド
```

- `host/mdx/build.sh` で mdxdump をビルド(third_party/portable_mdx 使用。
  sound_iocs.cpp に書き込みフックを追加してある)
- ADPCM(PDX)チャンネルは YM2151 単体では鳴らない(FM 8ch のみ)
- φM を 4MHz 以外で駆動する場合は `mdxplay.py --tune <実測Hz>` で
  KC/KF ピッチ補正(1/64半音単位)が掛かる

## 既知の問題

- **フル演奏中に電源起因のリセット(RST reason=1)が起きることがある。**
  アンプの消費電流で 5V レールがサグする疑い。対策候補: アンプ電源の別系統化、
  5V へのバルクコンデンサ(100-470µF)追加、ポリヒューズ(500mA)の見直し
- 4MHz 駆動時のキャプチャ採用率は約82%(φ1=2MHzに対するポーリング余裕減)。
  破棄フレームは直前値保持なので実害は小さいが、詰めるならループの最適化が必要
